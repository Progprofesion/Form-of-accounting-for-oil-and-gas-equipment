import { useForm } from "react-hook-form";
// import { useGetInputsQuery } from "../api/apiSlice";
import "./input.scss";
const Input = ({ type, className, placeholder, count }) => {
  // const {
  //   register,
  //   formState: { errors },
  //   handleSubmit,
  //   reset,
  // } = useForm({
  //   mode: "onBlur",
  // });
  // const { data: inputs, isLoading, isError } = useGetInputsQuery(null);
  // if (inputs) {
  //   return inputs.map((item) => {
  //     return (
  //       <input
  //         key={item.className}
  //         type={type}
  //         className={className}
  //         placeholder={placeholder}
  //       />
  //     );
  //   });
  // }
};

export default Input;
